{
  "settings": {
    "loglevel": "warning"
  },
  "menu": [
    {
      "type": "command",
      "title": "SleepScreen",
      "command": "bash -c 'gsettings set org.gnome.desktop.session idle-delay 1 && sleep 2 && gsettings set org.gnome.desktop.session idle-delay 0'",
      "icon": "system-lock-screen"
    }
  ]
}

