# Display default keywords

mkgitignore --display-default

# Use default keywords

mkgitignore --default
mkgitignore -d

# Set default keywords to "vim" and "ruby"
echo "keywords='vim,ruby'" > ~/.mkgitignorerc

# Use "default" + "rust" keywords

mkgitignore --default -k rust

# Use "vim" and "rust" keywords (You can use <TAB> completion)

`mkgitignore --keywords rust,vim`

or

`mkgitignore -k rust,vim`

# Display full list of valid keywords

mkgitignore --list

# Display a list of valid keywords matching the "ru" pattern (You can use <TAB> completion)

mkgitignore --list vu*
mkgitignore --list ru

# Upgrade to latest release

mkgitignore --upgrade

# Upgrade to a specific release

mkgitignore --upgrade 0.0.2

# Downgrade to the first release

mkgitignore --upgrade 0.0.1
